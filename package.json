{
  "name": "clickup-release-tagging-action",
  "version": "1.0.0",
  "description": "GitHub Action to tag ClickUp tasks with release names",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "action",
    "clickup",
    "release",
    "tag"
  ],
  "author": "Bravoure",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.1",
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
